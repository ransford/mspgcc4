mspgcc4 is not maintained in git.  Use the subversion repository:

  svn co https://mspgcc4.svn.sourceforge.net/svnroot/mspgcc4 mspgcc4 

The git repository contains the following branches used for staging changes
for integration into svn.

svn -- should mirror the contents of the trunk of the svn repository
next -- merge of all patches ready for integration
other -- independent feature branches containing patches
